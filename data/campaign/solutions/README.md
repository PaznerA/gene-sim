# Example solutions ("par")

One journal subdir per scenario index for `data/campaign/intro.json`, replayed by
`harness --campaign data/campaign/intro.json --journals data/campaign/solutions/intro`.
Each is a known WINNING action-journal — proof the scenario is solvable, and the par the
`shipped_intro_campaign_is_solvable` gate test asserts. Author your own to beat the par.
