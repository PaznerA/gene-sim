export const meta = {
  name: 'variant-lab-autoresearch-edits',
  description:
    'Variant Lab Slice D — give the brute-force AUTO-RESEARCH (the emergent-discovery search) the CRISPR-EDIT action, so the search explores the (init-config + MID-RUN-EDIT) space, not just initial configs (the user: the auto-research must ALSO have the edit/save/reseed actions). crates/discovery::search: add a SCHEDULED-EDITS axis to SearchConfig (a Vec of {gen, species_index, target_locus, guide} edit genes) as the LAST field with #[serde(default)] (so existing gitignored gems round-trip byte-identical), plus a deterministic std-only proposal/mutate of the edit schedule on NEW salted splitmix streams (disjoint from count/presence/env field indices) GATED behind a new SearchSpace knob edit_budget that DEFAULTS TO 0 — so the default search + every existing discovery test + the eval-log bytes stay byte-identical, and edits enter ONLY when a caller opts in. crates/harness::discover: thread the proposed edits into BOTH score_config (build the actions: Vec<(u32, Action::ApplyEdit)> passed to capture_trace, replacing the hardcoded &[]) AND verify_and_write_library (build the round-trip journal as the capture loop interleaves it: per gen, the scheduled ApplyEdits then Advance(1)), plus a CLI flag (e.g. --edit-budget) to enable the axis. The edits map to the EXISTING Slice-A Action::ApplyEdit(EditAction{ target, guide, species }) — no NEW sim action; the meta-RNG proposer is std+serde, biology stays in core. The pinned literal 0x47a0_3c8f_6701_f240 (single plant, no edits) is UNMOVED. Then gate + adversarially verify.',
  whenToUse: 'Variant Lab epic, after Slice A (per-species edit) + the discovery D2a/D2b/D3-A search infra. Makes the auto-research able to PROPOSE mid-run CRISPR edits as a search axis (the brute-force-auto-research gets the edit action).',
  phases: [{ title: 'SearchType' }, { title: 'Runner' }, { title: 'Gate' }, { title: 'Verify' }],
}

phase('SearchType')
const s1 = await agent(
  `Implement Variant Lab Slice D — STAGE 1, the SEARCH-SPACE type + proposer in crates/discovery (std+serde ONLY, inv #1/#5). READ FIRST: crates/discovery/src/search.rs IN FULL — SearchConfig (master_seed/roster/containment_level/temp_q/season), SpeciesAxis + SearchSpace + SearchSpace::default, the field-index allocation block (the FIXED "0=master_seed, 1+2*i=count, 2+2*i=presence, 1+2*N=containment, 2+2*N=temp, 3+2*N=season" comment + fi_count/fi_presence/fi_containment/fi_temp/fi_season), propose, the evolutionary operators (mutate/crossover/propose_evolved, MUTATE_SALT/CROSS_SALT/EVOLVE_SALT, op_draw), Gem, EvalRecord, and the tests (especially propose_is_byte_identical_for_same_seed_trial, propose_respects_space_bounds, mutate_is_deterministic, eval_record_serializes_field_in_declaration_order which pins the JSON prefix through roster). Also READ crates/harness/src/lib.rs:87 EditAction { target: LocusId, guide, species: u16 } + Action::ApplyEdit (the Slice-A per-species edit this axis targets) so the EditGene fields map onto a real ApplyEdit. CLAUDE.md inv #3 (determinism, integer/splitmix only, no rand crate, no HashMap iteration) + inv #5 (std+serde boundary).\n\n` +
  `  - Add a serde EditGene struct to search.rs: { gen: u32, species_index: u16, target: <the LocusId/u-int the EditAction.target uses>, guide: <the EditAction.guide repr> } — a DESCRIPTION of one mid-run edit (NO sim-core type; it stays on the clean std+serde side of the seam, like SearchConfig). Add edits: Vec<EditGene> as the LAST field of SearchConfig with #[serde(default)] so old gems (no field) deserialize to an empty schedule byte-identically, and the eval_record_serializes_field_in_declaration_order prefix (config.master_seed, roster, ...) is unchanged.\n` +
  `  - Add edit_budget: u8 (the max edits a proposed config may schedule) to SearchSpace, and set it to 0 in SearchSpace::default — THE CRITICAL HASH-NEUTRALITY KNOB: when edit_budget == 0, propose/mutate draw ZERO edit fields and produce edits: vec![], so the default search, every existing discovery test, and the eval-log bytes are BYTE-IDENTICAL. Edits enter ONLY when a caller raises edit_budget.\n` +
  `  - Extend propose (and mutate, for the evolutionary loop) to draw the edit schedule from NEW field indices ALLOCATED AFTER season (e.g. 4 + 2*N + k) and a NEW operator salt (an EDIT_SALT, like MUTATE_SALT) so the count/presence/env draws are UNPERTURBED (their field words do not shift) — adding the axis must not change any existing draw. Each edit gene draws gen in [0, gens) (or a [0,1) q16 fraction of the run if you prefer span-independence — your call, but document it), species_index over the roster, target/guide over the EditAction domains. Clamp everything; an empty/absent schedule when edit_budget==0.\n` +
  `  - Tests: (a) propose/mutate with the DEFAULT space (edit_budget 0) is byte-identical to before — assert edits is empty AND that a fixed (seed, trial) still yields the same roster/env as the existing tests expect (the existing tests must pass unchanged). (b) With edit_budget > 0, propose is deterministic (same seed,trial -> identical edits) + in-bounds (gen < gens, species_index < roster len, target/guide in range) + the schedule length <= edit_budget. (c) SearchConfig with edits round-trips serde, and a SearchConfig JSON written WITHOUT the edits field still deserializes (serde-default).\n` +
  `  - VERIFY the pinned literal 0x47a0_3c8f_6701_f240 is UNMOVED (discovery has no sim dep; run cargo test -p discovery). Do NOT commit. Report the EditGene + edits field + edit_budget knob + the new field-index/salt allocation + confirm the default-space byte-identity tests pass.`,
  { label: 'search-type', phase: 'SearchType', agentType: 'implementer' },
)

phase('Runner')
const s2 = await agent(
  `Implement Variant Lab Slice D — STAGE 2, thread the proposed edits through the harness runner, on the Stage-1 search type:\n${typeof s1 === 'string' ? s1.slice(0, 800) : ''}\n\n` +
  `READ crates/harness/src/discover.rs IN FULL — env_config_for, score_config (note line ~151 capture_trace(&mut env, cfg.master_seed, gens, &[]) — the hardcoded EMPTY actions this slice replaces; the comment there literally says "NO journaled actions ... mid-run edits are a later axis" — THIS IS that axis), capture_and_consider, verify_and_write_library (note the journal = vec![Action::Advance(gem.gens)] at ~line 326 + the round-trip contract record_episode -> replay == recorded_hash), discover + discover_evolved (the CLI-facing runners + their args), and the BUILD_ID. READ crates/harness/src/capture.rs capture_trace (it ALREADY takes actions: &[(u32, Action)] and, per generation g in 1..=gens, applies every non-Advance action scheduled for g at the TOP of the step, then Advance(1) — so the journal that reproduces it is, per gen: the scheduled ApplyEdits, then Advance(1)). READ crates/harness/src/main.rs (the --discover/--evolve CLI flags) + crates/harness/src/lib.rs Action::ApplyEdit/EditAction. Inv #3 (the edited run must round-trip) + inv #2 (the gate runs in sim-core; the harness only schedules the existing Action).\n\n` +
  `  - Add a helper that turns cfg.edits (Vec<EditGene>) into actions: Vec<(u32, Action)> — each EditGene -> (gen, Action::ApplyEdit(EditAction { target, guide, species: <roster species_index resolved to the SpeciesId u16 the env uses> })). Resolve species_index against the SAME ordered roster env_config_for builds (Slice A's species:u16 boundary). Pass these actions to capture_trace in score_config (replace the &[]).\n` +
  `  - In verify_and_write_library, build the round-trip journal to MATCH capture_trace's interleave: for gen in 1..=gem.gens { for each edit scheduled at gen (in list order) push Action::ApplyEdit(..); push Action::Advance(1) } — byte-identical to capture_trace's stepping (Advance(1) per gen with edits applied at the top), so the edited gem still round-trips (record_episode -> replay == recorded_hash). A config with NO edits must collapse to EXACTLY the current journal behaviour (one Advance(g) is byte-identical to Advance(1)*g per the existing tests/trace_capture.rs note) so the existing gem round-trip tests are unchanged.\n` +
  `  - Wire a CLI flag (e.g. --edit-budget N) that sets SearchSpace.edit_budget for the discover/discover_evolved runners (default 0 -> the existing behaviour byte-identical). Keep the default space at edit_budget 0 everywhere a runner does not opt in.\n` +
  `  - Tests: (a) with edit_budget 0, discover/discover_evolved produce byte-identical gems + eval-logs to before (the existing discover_evolved.rs / discover.rs tests must pass unchanged). (b) with edit_budget > 0, a discovered gem whose config carries edits ROUND-TRIPS (record_episode -> replay == recorded_hash == gem.recorded_hash) — the edited run is reproducible. (c) the same (search_seed, edit_budget) yields byte-identical saved gems (determinism).\n` +
  `  - VERIFY the pinned literal 0x47a0_3c8f_6701_f240 is UNMOVED (cargo test -p sim-core determinism; the single-plant pinned config has no edits). Build the workspace. Do NOT commit. Report the actions-builder + the verify-journal interleave + the CLI flag + confirm the edit_budget-0 byte-identity tests pass + an edited gem round-trips + 0x47a0 unmoved.`,
  { label: 'runner', phase: 'Runner', agentType: 'implementer' },
)

phase('Gate')
const gate = await agent(
  `Run \`bash tools/gate.sh\` for gene-sim (generous timeout ~15 min). Variant Lab D (auto-research edit axis) must be GREEN: fmt, clippy, test (incl. the discovery propose/mutate edit-axis tests + the harness edited-gem round-trip tests), determinism MUST be GREEN against the pinned literal 0x47a0_3c8f_6701_f240 (a moved hash is a FAIL — the edit axis is meta-level + default-OFF, so the literal must be byte-identical), license green (discovery stays std+serde — no new deps), godot-reader + livesim green. Report every gate PASS/FAIL with exact errors. No fixes, no commit.`,
  { label: 'gate', phase: 'Gate', agentType: 'gatekeeper' },
)

phase('Verify')
const VSCHEMA = {
  type: 'object',
  required: ['edit_axis_gated_off_by_default', 'edits_thread_as_existing_applyedit', 'edited_run_round_trips', 'proposal_deterministic_in_bounds', 'serde_default_byte_neutral', 'issues'],
  properties: {
    edit_axis_gated_off_by_default: { type: 'boolean', description: 'inv #3: SearchSpace::default has edit_budget == 0, so propose/mutate draw ZERO edit fields, produce edits: vec![], and the existing discovery tests + eval-log bytes are byte-identical; the new edit field indices/salt are allocated AFTER season so no existing count/presence/env draw shifts; the pinned literal 0x47a0_3c8f_6701_f240 is UNMOVED.' },
    edits_thread_as_existing_applyedit: { type: 'boolean', description: 'inv #2: each EditGene maps to the EXISTING Slice-A Action::ApplyEdit(EditAction{ target, guide, species }) threaded into capture_trace.actions + the verify journal — NO new sim action, no biology in the meta-RNG proposer; the ApplyEdit gate runs in sim-core; species_index resolves against the same ordered roster.' },
    edited_run_round_trips: { type: 'boolean', description: 'inv #3: the verify journal interleaves the scheduled ApplyEdits then Advance(1) per gen to MATCH capture_trace, so an edited gem round-trips record_episode -> replay == recorded_hash == gem.recorded_hash; a no-edit config collapses to the unchanged one-Advance journal.' },
    proposal_deterministic_in_bounds: { type: 'boolean', description: 'inv #3: with edit_budget > 0 the edit schedule is drawn from salted splitmix streams (no rand crate, no HashMap iteration); same (seed, trial/step) -> identical schedule; gen/species_index/target/guide are clamped in-bounds and the schedule length <= edit_budget.' },
    serde_default_byte_neutral: { type: 'boolean', description: 'SearchConfig.edits is the LAST field with #[serde(default)]: an old gem JSON without the field deserializes to an empty schedule, and the eval_record_serializes_field_in_declaration_order JSON-prefix test still passes (the surrogate training-data contract is intact).' },
    issues: { type: 'array', items: { type: 'string' } },
  },
}
const skeptics = (await parallel([0, 1, 2].map((i) => () =>
  agent(
    `Adversarially verify Variant Lab Slice D (the auto-research mid-run-edit search axis). Read \`git diff\` (crates/discovery/src/search.rs + crates/harness/src/discover.rs + capture.rs + main.rs), and CLAUDE.md inv #2/#3/#5. Skeptic #${i} — default each boolean FALSE unless you confirm it from the diff. Hunt: a MOVED pinned literal 0x47a0_3c8f_6701_f240; a SHIFTED field-index/draw that perturbs the existing count/presence/env proposals (the edit fields MUST be allocated after season + on a disjoint salt, so edit_budget 0 is byte-identical) — check the existing propose/mutate determinism tests still hold; an edits field NOT last / NOT serde-default (would break old-gem round-trip + the JSON-prefix test); a verify journal that does NOT match capture_trace's interleave (would break the edited-gem round-trip — record/replay mismatch); a NEW sim Action or biology computed in the std+serde proposer (must reuse Action::ApplyEdit; biology stays in sim-core); a rand-crate/HashMap-iteration/float creeping into the proposer; the default space NOT at edit_budget 0 (would silently change the default search + every gem). Report the structured verdict with file:line. Do NOT edit.`,
    { label: `verify:skeptic${i}`, phase: 'Verify', schema: VSCHEMA, agentType: 'reviewer' },
  ),
))).filter(Boolean)
const tally = (k) => skeptics.filter((s) => s[k]).length
const keys = ['edit_axis_gated_off_by_default', 'edits_thread_as_existing_applyedit', 'edited_run_round_trips', 'proposal_deterministic_in_bounds', 'serde_default_byte_neutral']
const confirmed = keys.every((k) => tally(k) >= 2)
return {
  search_type: typeof s1 === 'string' ? s1.slice(0, 700) : s1,
  runner: typeof s2 === 'string' ? s2.slice(0, 700) : s2,
  gate: typeof gate === 'string' ? gate.slice(0, 500) : gate,
  tallies: Object.fromEntries(keys.map((k) => [k, tally(k)])),
  all_issues: skeptics.flatMap((s) => s.issues || []),
  verdict: confirmed ? 'CONFIRMED — auto-research gains the mid-run-edit axis; default-OFF + byte-identical to 0x47a0; edited gems round-trip' : 'NEEDS WORK',
}
