export const meta = {
  name: 'plant-realism-impl',
  description:
    'ADR-029 S5 (renderer-only): plant colonies — always-visible + most-realistic. (1) ALWAYS-VISIBLE FLOOR: a plant colony never decays to a haze speck (it skips the sub-MIN_COLONY_CELLS haze path) and never collapses below fill+outline at any zoom — a minimum on-screen alpha + size floor (the full-pop ghost floors ABOVE the microbe GHOST_FILL_FACTOR 0.15 so the plant district frame always reads). (2) MORPH-AWARE AGGREGATE: plant colonies draw as a SOFT CANOPY HULL (organic rounder boundary = extra Chaikin smoothing, a green gradient fill by mean fitness/density) while microbe colonies keep the HARD-EDGED Cities-Skylines district (the current single-Chaikin contour) — branch on is_plant from the species table. (3) >=1-COLONY GUARANTEE: every non-empty plant cell lands in exactly one connected-component colony (structural — every cell has a dominant species/variant); assert it with a headless test (committed + wired into the gate like colony_s4_test.gd). (4) Plants POP FIRST to L-system trees (already true via SIZE_PLANT 2.2 in the S3 footprint) — confirm the popped plant uses organisms.gd _draw_plant (the L-system canopy), untouched. ZERO RUST (renderer-only): pinned literal 0x47a0_3c8f_6701_f240 trivially unmoved; inv #2 (presentation only — no genotype->phenotype) + inv #3 (ordered passes, no RNG, no per-frame redraw). Read docs/llm/proposals/visual-declutter-colony-draft.md section 4.3 + section 7 S5 first. Then gate + adversarially verify.',
  whenToUse: 'After ADR-029 S4. Makes plant colonies always-visible + canopy-realistic (the brief: plants most-realistic + always-visible + >=1-colony), the last required colony slice before the optional S6 polish.',
  phases: [{ title: 'Impl' }, { title: 'Gate' }, { title: 'Verify' }],
}

phase('Impl')
const s1 = await agent(
  'Implement ADR-029 S5 — plant-realism (RENDERER-ONLY GDScript; ZERO Rust; the pinned literal 0x47a0_3c8f_6701_f240 stays trivially unmoved). READ FIRST: docs/llm/proposals/visual-declutter-colony-draft.md section 4.3 (plants always-visible / most-realistic / >=1-colony) + section 7 S5 (the exact spec). Then READ the REAL surface: godot/colonies.gd (the _species_table is_plant + morph per species ~:51; the haze-speck path count < MIN_COLONY_CELLS ~:254 + HAZE_ALPHA ~:30; the district build with loops/_fill_color/FILL_ALPHA ~:281-298; the full-pop ghost GHOST_FILL_FACTOR 0.15 ~:39 + POP_HI_PX; _chaikin_once ~:383 — ONE pass today = the hard edge; _fill_color ~:399; the S3 footprint/pop_t), godot/organisms.gd (the L-system plant canopy: _compute_template ~:109-155 — branchiness/reflectance/canopy; _draw_plant ~:259 — the popped plant glyph, REUSE untouched; the S3/S4 pop gate + selected-pop), godot/species_visual_map.gd (is_plant / morph_for / SIZE_PLANT 2.2 ~:21 / COLOR_PLANT), godot/colony_s4_test.gd + tools/check_godot_snapshot.sh (the committed headless-test pattern to follow). CLAUDE.md inv #2 (render-only — NO genotype->phenotype; the plant biology stays in core) + inv #3 (renderer determinism: ordered passes, no Dictionary/hash-order iteration, no randf/randi/Time/OS, NO per-frame redraw).\n\n' +
  '  - ALWAYS-VISIBLE FLOOR: for a PLANT colony (is_plant from the species table), SKIP the sub-MIN_COLONY_CELLS haze-speck path — a plant always renders as a full district (fill + outline) even at 1-2 cells, with a minimum on-screen alpha + size floor so it never decays into background haze. At full pop the plant ghost fill floors ABOVE the microbe GHOST_FILL_FACTOR (e.g. a higher PLANT_GHOST_FILL_FACTOR) so the plant district frame is always visible at every zoom. Microbe behaviour is unchanged.\n' +
  '  - CANOPY HULL vs HARD DISTRICT (morph-aware): plant colonies draw as a SOFT canopy hull — a rounder/organic boundary (extra Chaikin smoothing pass(es) on the plant contour) + a green GRADIENT fill (gradient by mean fitness/density — a soft canopy mass, not a flat zone). Microbe colonies keep the current HARD-EDGED district (single Chaikin, clean Cities-Skylines boundary). Branch on is_plant; keep both paths deterministic (ordered, no hash-order). Reuse the species canopy palette (COLOR_PLANT / the visual table) so plants read as vegetation green.\n' +
  '  - >=1-COLONY GUARANTEE: confirm every non-empty plant cell lands in a colony (label != background) — structural since every cell has a dominant species/variant. Add a headless test (a new godot/colony_s5_test.gd OR extend colony_s4_test.gd) asserting: every non-empty plant cell has a colony label; a sub-MIN_COLONY_CELLS plant colony renders as a DISTRICT (haze:false) not a haze speck; the plant ghost-fill floor > the microbe GHOST_FILL_FACTOR. WIRE it into tools/check_godot_snapshot.sh exactly like colony_s4_test.gd (and COMMIT the test file — a referenced-but-missing test reddens the gate).\n' +
  '  - POP-FIRST TO L-SYSTEM TREES: plants already pop first (SIZE_PLANT 2.2 in the S3 footprint). Confirm the popped plant cell uses organisms.gd _draw_plant (the L-system canopy) untouched — do NOT duplicate plant morphology in colonies.gd (single source of truth). colonies.gd owns only the aggregate hull/fill/label.\n' +
  '  - inv #2: the canopy hull / gradient / always-visible floor are PRESENTATION. No genotype->phenotype in GDScript; the canopy SHAPE driver (branchiness/reflectance) already comes from the core species traits via the existing template — S5 adds no biology.\n' +
  '  - BUILD + macOS-SAFE smoke: build the cdylib (must still build with ZERO Rust diff). Run a macOS-SAFE --shot (timeout + FILE capture, never a $(godot...) pipe; WINDOWED; SKIP cleanly + say so if no display) on a sample run with BOTH a plant species + a microbe species at a zoomed-OUT scope: CONFIRM the plant colony shows as a soft canopy hull beside a hard-edged microbe district, and a plant district is visible (not haze) at the most zoomed-out scope. If no display, prove it at the code level + run colony_s5_test.gd / livesim_smoke.gd (the project loads + the test passes).\n' +
  '  - CONFIRM zero Rust diff: git diff --stat shows NO crates/ change. Do NOT commit. Report: the always-visible floor (the plant haze skip + the plant ghost-fill floor value), the canopy-hull vs hard-district branch, the >=1-colony test + its gate wiring (+ that the test file is created), the pop-first-to-_draw_plant confirmation, and confirm no per-frame redraw + zero Rust diff.',
  { label: 'impl', phase: 'Impl', agentType: 'implementer' },
)

phase('Gate')
const gate = await agent(
  'Run bash tools/gate.sh for gene-sim (generous timeout ~15 min). ADR-029 S5 is RENDERER-ONLY — it must be GREEN: fmt, clippy, test, determinism MUST stay 0x47a0_3c8f_6701_f240 (S5 touches ZERO Rust — confirm git diff --stat shows no crates/ change, so the literal is byte-identical by construction), the godot snapshot byte gate (still GSS6/channels=14, AND the new colony_s5 plant test passes — confirm the referenced test file EXISTS so the gate is not reddened by a missing --script target), license green, godot-reader + livesim_smoke green. Report every gate PASS/FAIL with exact errors + EXPLICITLY confirm 0x47a0 unmoved + zero Rust diff + the plant test ran. No fixes, no commit.',
  { label: 'gate', phase: 'Gate', agentType: 'gatekeeper' },
)

phase('Verify')
const VSCHEMA = {
  type: 'object',
  required: ['renderer_only_zero_rust_hash_unmoved', 'plants_always_visible_floor', 'canopy_hull_vs_hard_district', 'one_colony_guarantee_and_no_biology', 'issues'],
  properties: {
    renderer_only_zero_rust_hash_unmoved: { type: 'boolean', description: 'S5 is GDScript-only: git diff shows NO crates/ (Rust) change; the pinned literal 0x47a0_3c8f_6701_f240 is byte-identical by construction; snapshot stays GSS6/channels=14; any new headless test file is committed + wired into the gate (no missing --script target).' },
    plants_always_visible_floor: { type: 'boolean', description: 'A plant colony NEVER decays to a haze speck (it skips the sub-MIN_COLONY_CELLS haze path — renders as a district even at 1-2 cells) and never collapses below fill+outline at any zoom (a minimum alpha/size floor; the plant full-pop ghost fill floors ABOVE the microbe GHOST_FILL_FACTOR 0.15). Asserted by the committed headless test.' },
    canopy_hull_vs_hard_district: { type: 'boolean', description: 'Morph-aware aggregate: plant colonies draw as a SOFT canopy hull (extra Chaikin smoothing + a green gradient fill) while microbe colonies keep the HARD-EDGED Cities-Skylines district (single Chaikin). Plants pop first to the L-system trees via organisms.gd _draw_plant (reused untouched — no duplicated plant morphology in colonies.gd).' },
    one_colony_guarantee_and_no_biology: { type: 'boolean', description: '>=1-colony guarantee: every non-empty plant cell lands in exactly one connected-component colony (asserted by the headless test). inv #2: the floor/hull/gradient are presentation only — no genotype->phenotype in GDScript. inv #3: ordered passes (no Dictionary/hash-order iteration), no randf/randi/Time/OS, no per-frame redraw.' },
    issues: { type: 'array', items: { type: 'string' } },
  },
}
const skeptics = (await parallel([0, 1, 2].map((i) => () =>
  agent(
    'Adversarially verify ADR-029 S5 (plant-realism — renderer-only). Read git diff (godot/colonies.gd + maybe organisms.gd + the new colony_s5 test + tools/check_godot_snapshot.sh) + docs/llm/proposals/visual-declutter-colony-draft.md section 4.3 + section 7 S5 + CLAUDE.md inv #2/#3. Skeptic #' + i + ' — default each boolean FALSE unless PROVEN. Hunt: ANY crates/ (Rust) diff (must be GDScript-only — if Rust changed, confirm 0x47a0_3c8f_6701_f240 byte-identical and flag it); a headless test referenced in check_godot_snapshot.sh but NOT created/committed (would redden a fresh-checkout gate — the S4 lesson); a plant colony that STILL collapses to a haze speck or below fill+outline at some zoom (the always-visible floor missing); plant + microbe colonies drawn identically (no canopy-hull vs hard-district distinction); plant morphology DUPLICATED in colonies.gd instead of reusing organisms.gd _draw_plant; a per-frame redraw (a _process/Timer); Dictionary/hash-order iteration or randf/randi/Time/OS (renderer non-determinism); biology computed in GDScript (inv #2). Report the structured verdict with file:line. Do NOT edit.',
    { label: 'verify:skeptic' + i, phase: 'Verify', schema: VSCHEMA, agentType: 'reviewer' },
  ),
))).filter(Boolean)
const tally = (k) => skeptics.filter((s) => s[k]).length
const keys = ['renderer_only_zero_rust_hash_unmoved', 'plants_always_visible_floor', 'canopy_hull_vs_hard_district', 'one_colony_guarantee_and_no_biology']
const confirmed = keys.every((k) => tally(k) >= 2)
return {
  impl: typeof s1 === 'string' ? s1.slice(0, 800) : s1,
  gate: typeof gate === 'string' ? gate.slice(0, 600) : gate,
  tallies: Object.fromEntries(keys.map((k) => [k, tally(k)])),
  all_issues: skeptics.flatMap((s) => s.issues || []),
  verdict: confirmed ? 'CONFIRMED — plants always-visible (canopy hull, floored) + >=1-colony; renderer-only, 0x47a0 byte-identical' : 'NEEDS WORK',
}
