export const meta = {
  name: 'brush-colony-binding-impl',
  description:
    'ADR-029 S4 (renderer-only — the CORE brush->variant bind already shipped in S1; this is its render SURFACE): the CRISPR brush reads as the Cities-Skylines "create district" verb. (1) Render the brushed disc as a NESTED sub-district INSIDE the parent species territory — close the S2 hole-cut deferral (cut the child variant region OUT of the parent polygon fill so the parent reads as a frame around the child) + the bounded intra-species hue shift (S2 _variant_hue_shift) so the new district reads as FAMILY, not a foreign species. (2) A renderer-side colony registry variant_id -> {species, label, color, gen_created, parent} assembled from observe_species() + the renderer\'s own journaled ApplyEditRegion brush strokes (the new variant id appears in the next snapshot dominant_variant_id channel within the brushed disc). (3) world-click -> set_selected_colony(id): force that district to POP OPEN regardless of zoom (a _selected_colony_id override of the S3 footprint test) while neighbours stay aggregated — CAPPED to the viewport rect / a per-colony sprite budget so a map-spanning species cannot re-spam (basic cap here; S6 hardens). (4) The brushed-disc --shot that closes the S2 acceptance bullet. ZERO RUST (renderer-only — if a core #[func] seems unavoidable, STOP and report rather than adding one; the draft asserts S4 is renderer-only): pinned literal 0x47a0_3c8f_6701_f240 trivially unmoved; inv #2 (registry/hue/selection are PRESENTATION — no genotype->phenotype) + inv #3 (registry + selection built in ordered passes, no hash-order iteration, no RNG, no per-frame redraw). Read docs/llm/proposals/visual-declutter-colony-draft.md section 3 + section 7 S4 first. Then gate + adversarially verify.',
  whenToUse: 'After ADR-029 S3 (the LOD pop ladder). The render surface of the brush->district bind: a brush stroke visibly creates a nested district that reads as family + persists/moves with its organisms; click selects + pops a district.',
  phases: [{ title: 'Impl' }, { title: 'Gate' }, { title: 'Verify' }],
}

phase('Impl')
const s1 = await agent(
  'Implement ADR-029 S4 — the brush->colony render surface (RENDERER-ONLY GDScript; the CORE bind shipped in S1; ZERO Rust; the pinned literal 0x47a0_3c8f_6701_f240 stays trivially unmoved). READ FIRST: docs/llm/proposals/visual-declutter-colony-draft.md section 3 (BRUSH-CREATES-COLONY — the create-district verb) + section 7 S4 (the exact spec) + section 4.2 selected-pop bullet (the cap). Then READ the REAL surface: godot/colonies.gd (S2/S3: the per-cell key species_id*65536+variant_id; _variant_hue_shift ~:409; _trace_boundary ~:259 which currently returns ONLY the outer hull — the hole-cut to close; the per-colony _colony_draw entries; the set_zoom/footprint from S3), godot/organisms.gd (the S3 per-cell pop gate keyed on footprint — the selected-pop must override it for the selected colony), godot/main.gd (the existing brush: _brush/_set_brush_mode ~:224-229/477, TOOL_CRISPR via apply_edit_region, the --brush demo-stroke flag ~:467-480, observe_species feed ~:129, the click/_input handling, _show ~:5396 feeds both layers), godot/brush.gd (the brush overlay). godot-sim apply_edit_region + observe_species are EXISTING #[func]s — do NOT add a new one (S4 is renderer-only). CLAUDE.md inv #2 (render-only — registry/hue/selection are presentation, NO genotype->phenotype) + inv #3 (renderer determinism: registry + selection built in ORDERED passes, no Dictionary/hash-order iteration, no randf/randi/Time/OS, NO per-frame redraw — queue_redraw only on state change).\n\n' +
  '  - NESTED DISTRICT (close the S2 hole-cut): render the brushed-variant region as a sub-polygon INSIDE its parent species territory — cut the child region OUT of the parent polygon fill (a hole / even-odd or a separate masked draw) so the parent reads as a frame around the child, and the child draws with the bounded intra-species hue shift (reuse _variant_hue_shift so it reads as FAMILY of the same species, NOT a foreign species). Keep the determinism (row-major / ordered; no hash-order iteration).\n' +
  '  - COLONY REGISTRY (renderer-side, presentation): a variant_id -> {species, label, color, gen_created, parent} map assembled from observe_species() + the renderer\'s OWN journaled ApplyEditRegion brush strokes (when the player fires a brush you know the gen, the species, the disc, and the prior dominant variant in the disc = parent; the minted child variant id appears in the next snapshot dominant_variant_id channel inside the disc). Build it in ordered passes (no hash-order iteration). Used for the district label (name . variant . count) + a stable color.\n' +
  '  - SELECTED-POP: world-click -> set_selected_colony(id) (store _selected_colony_id; clear on click-empty). The selected colony POPS OPEN regardless of zoom (override the S3 footprint test for its cells in organisms.gd) while neighbours stay aggregated. CAP it: clamp the popped sprites to the viewport rect AND/OR a per-colony sprite budget so a map-spanning selected colony cannot re-spam the screen (basic cap here; S6 hardens the perf cap). Deterministic, no per-frame redraw — queue_redraw only on the click/zoom/scope/state change.\n' +
  '  - inv #2: the registry, hue shift, hole-cut and selection are ALL presentation/geometry. No genotype->phenotype computed in GDScript. The brush still calls the EXISTING apply_edit_region #[func] (the S1 core bind) — no new wire, no new core func.\n' +
  '  - BUILD + macOS-SAFE smoke: build the cdylib (must still build with ZERO Rust diff). Run a macOS-SAFE --shot (timeout + FILE capture, never a $(godot...) pipe; WINDOWED; SKIP cleanly + say so if no display) using the --brush demo-stroke path: capture a frame AFTER a brush stroke at a zoom where the parent territory is a polygon, and CONFIRM the brushed disc shows as a NESTED sub-district (the S2 deferred acceptance bullet) tinted as family; then a second frame after a step or two to confirm the district PERSISTS / moves with its organisms (the heritable S1 Variant tag). If no display, prove it at the code level (hole-cut geometry + the heritable-tag persistence) + run livesim_smoke.gd so the project loads. Demonstrate a click selects + pops a district (or prove the set_selected_colony override path).\n' +
  '  - CONFIRM zero Rust diff: git diff --stat shows NO crates/ change. Do NOT commit. Report: the hole-cut approach, the registry assembly (proving ordered/no-hash-order), the selected-pop override + its re-spam cap, the brushed --shot evidence (or code path), and confirm no per-frame redraw + zero Rust diff.',
  { label: 'impl', phase: 'Impl', agentType: 'implementer' },
)

phase('Gate')
const gate = await agent(
  'Run bash tools/gate.sh for gene-sim (generous timeout ~15 min). ADR-029 S4 is RENDERER-ONLY — it must be GREEN: fmt, clippy, test, determinism MUST stay 0x47a0_3c8f_6701_f240 (S4 touches ZERO Rust — confirm git diff --stat shows no crates/ change, so the literal is byte-identical by construction), the godot snapshot byte gate (still GSS6/channels=14), license green, godot-reader + livesim_smoke green (the modified colonies.gd + organisms.gd + main.gd must load + run). Report every gate PASS/FAIL with exact errors + EXPLICITLY confirm 0x47a0 unmoved + zero Rust diff. No fixes, no commit.',
  { label: 'gate', phase: 'Gate', agentType: 'gatekeeper' },
)

phase('Verify')
const VSCHEMA = {
  type: 'object',
  required: ['renderer_only_zero_rust_hash_unmoved', 'brush_creates_nested_family_district_persists', 'selected_pop_capped_no_respam', 'no_biology_no_renderer_nondeterminism', 'issues'],
  properties: {
    renderer_only_zero_rust_hash_unmoved: { type: 'boolean', description: 'S4 is GDScript-only: git diff shows NO crates/ (Rust) change and NO new core #[func]; the pinned literal 0x47a0_3c8f_6701_f240 is byte-identical by construction; snapshot stays GSS6/channels=14; the brush still calls the EXISTING apply_edit_region (the S1 core bind).' },
    brush_creates_nested_family_district_persists: { type: 'boolean', description: 'A brush stroke renders the edited disc as a NESTED sub-district INSIDE the parent territory (the S2 hole-cut is closed — the child region is cut out of the parent fill) with a bounded intra-species hue shift so it reads as FAMILY (not a foreign species); the district PERSISTS / moves with its organisms across steps (the heritable S1 Variant tag). Proven by a brushed --shot OR the hole-cut + heritable-tag code path.' },
    selected_pop_capped_no_respam: { type: 'boolean', description: 'world-click -> set_selected_colony(id) forces that district to POP OPEN regardless of zoom (overriding the S3 footprint test for its cells) while neighbours stay aggregated, CAPPED to the viewport rect / a per-colony sprite budget so a map-spanning selected colony cannot re-spam. Deterministic, no per-frame redraw.' },
    no_biology_no_renderer_nondeterminism: { type: 'boolean', description: 'inv #2: the registry / hue shift / hole-cut / selection are PRESENTATION ONLY — no genotype->phenotype in GDScript. inv #3 in the renderer: the registry + selection are built in ordered passes (no Dictionary/hash-order iteration), no randf/randi/Time/OS, queue_redraw only on state change (no _process/Timer).' },
    issues: { type: 'array', items: { type: 'string' } },
  },
}
const skeptics = (await parallel([0, 1, 2].map((i) => () =>
  agent(
    'Adversarially verify ADR-029 S4 (the brush->colony render surface — renderer-only). Read git diff (godot/colonies.gd + godot/organisms.gd + godot/main.gd + maybe brush.gd) + docs/llm/proposals/visual-declutter-colony-draft.md section 3 + section 7 S4 + CLAUDE.md inv #2/#3. Skeptic #' + i + ' — default each boolean FALSE unless PROVEN. Hunt: ANY crates/ (Rust) diff or a NEW core #[func] (S4 must be GDScript-only reusing apply_edit_region — if Rust changed, confirm 0x47a0_3c8f_6701_f240 byte-identical and flag it); the brushed disc NOT rendered as a nested district (hole-cut missing -> the child is hidden under the parent fill, OR it reads as a foreign species instead of a family hue shift); the district NOT persisting with its organisms; a selected-pop that re-spams (no viewport/sprite-budget cap on a map-spanning selected colony); a per-frame redraw (a _process/Timer calling queue_redraw); registry/selection iterating a Dictionary / hash order (renderer non-determinism) or randf/randi/Time/OS; biology computed in GDScript (inv #2). Report the structured verdict with file:line. Do NOT edit.',
    { label: 'verify:skeptic' + i, phase: 'Verify', schema: VSCHEMA, agentType: 'reviewer' },
  ),
))).filter(Boolean)
const tally = (k) => skeptics.filter((s) => s[k]).length
const keys = ['renderer_only_zero_rust_hash_unmoved', 'brush_creates_nested_family_district_persists', 'selected_pop_capped_no_respam', 'no_biology_no_renderer_nondeterminism']
const confirmed = keys.every((k) => tally(k) >= 2)
return {
  impl: typeof s1 === 'string' ? s1.slice(0, 800) : s1,
  gate: typeof gate === 'string' ? gate.slice(0, 600) : gate,
  tallies: Object.fromEntries(keys.map((k) => [k, tally(k)])),
  all_issues: skeptics.flatMap((s) => s.issues || []),
  verdict: confirmed ? 'CONFIRMED — brush creates a nested family district (persists/moves) + capped selected-pop; renderer-only, 0x47a0 byte-identical' : 'NEEDS WORK',
}
